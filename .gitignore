# kas checks the layer repositories out here ...
/layers/
# ... and bitbake works here.
/build/
# Shared between builds, so kept out of build/ (see Building in the README).
/downloads/
/sstate-cache/

# kas menu / kas-container scratch
/.config.yaml
/.kas-buildtools/

# mkdocs build output
/site/

.kas_shell_history
